# BIS Standards Recommendation Engine — Dependencies
# Core: pure Python stdlib only for the RAG retrieval pipeline.
# No external ML libraries required for inference.py to run.
# Optional: anthropic SDK for LLM rationale generation.

# ── Optional: LLM rationale generation (uncomment if using API) ──
# anthropic>=0.25.0

# ── Optional: PDF ingestion from BIS SP 21 source documents ──
# pdfplumber>=0.10.0
# pymupdf>=1.23.0        # also known as fitz

# ── Optional: Vector store for production-scale RAG ──
# chromadb>=0.4.0
# sentence-transformers>=2.6.0
# faiss-cpu>=1.7.4

# ── Dev/Eval only ──
# pytest>=7.4.0
